scenario_id: acc_csc_022_002
logic:
  scenario_id: acc_csc_022
  name: EV in FL mode + TV stops + TV resumes movement
  functional: ACC
  feature_domain: Longitudinal
  pre_condition:
    ev_mode: FL
  actors:
    ev:
      type: ego
      lane: ego
      state: moving
    tv:
      type: car
      lane: ego
      position: front_ego
      state: moving
  maneuvers:
  - type: stop_event
    actor: tv
parameters:
  ev_speed: 30
  tv_speed: 20
  light: day
  curve_radius: 1000
