scenario_id: acc_csc_005
name: FL mode cut in left
functional: ACC
feature_domain: Longitudinal

pre_condition:
  ev_mode: FL

actors:
  - ev: 
      type: ego
      lane: ego
      state: moving
  - tv_old: 
      type: car
      lane: ego
      position: front_ego
      state: moving
  - tv:
      type: bus
      lane: left
      position: next_to_ego
      state: moving

maneuvers:
  - type: cutin_event
    actor: tv